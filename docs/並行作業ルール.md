# 並行作業ルール（施策学習サイト担当Claude 向け）

最終更新: 2026-07-16（ファイル構造刷新に伴い担当レーンを更新）

> `~/Desktop/CTA　デザイン` フォルダでは、**同時にもう1つのClaude**が別作業（CTA-14019 記事TOPの**Figmaデザイン**）を進めていることがあります。
> ファイル・Figma・共有ドキュメントの**衝突事故を防ぐ**ため、あなた（施策学習サイト担当）は以下のレーンとルールを必ず守ってください。
> あなたの作業の全体像は同じ `docs/引き継ぎ書.md` を参照。本ファイルは「他Claudeと衝突しないための追加ルール」です。

> ⚠️ **2026-07-16 構造刷新**: 施策学習サイトの「正」は `~/Desktop/cta-measure-learnings`（GitHubリポジトリ）に一本化されました。旧 `CTA　デザイン/docs/measures/施策学習サイト/` および `CTA　デザイン/docs/measures/CTA-{番号}/` のミラーは廃止です（各所にREADMEの案内あり）。

## 0. まず作業開始時に必ずやること（チェックリスト）

1. `docs/引き継ぎ書.md` を読む（自分の作業内容の把握）
2. 本ファイル（並行作業ルール）を読む
3. `cta-measure-learnings` リポジトリで `git log --oneline -5` とローカル差分を確認。**リポジトリ側が進んでいたら先にpull/確認**
4. Figmaには触らない前提で作業を始める（下記ルール2）

## 1. あなたの担当（触ってよい範囲）

- `cta-measure-learnings/`（施策学習サイトの正・唯一の管理対象。commit/push はここだけ）
  - `index.html` / `assets/css/style.css` / `assets/js/app.js` / `data/measures.js` / `images/` / `measures/*.md` / `docs/*`
- `~/Desktop/CTA　デザイン/.claude/references/measure-domain-knowledge.md` と `.claude/references/measure-types/*`（施策レビュー観点の追記。存在する場合）

## 2. 触ってはいけない範囲（デザイナー側Claudeの担当）

- ❌ **Figmaファイルへの書き込みは一切しない。**
  - Figma fileKey `LfO02MZYK6spGnMHj1x2E7`（CTA-14019 記事TOP）は、もう一方が編集中。ページ `AI_記事TOP_PC_20260714`、フレーム `article-top-PC` / `article-top-PC-修正` などに**絶対に書き込まない**。
  - スクショ取得などの**読み取りは可**。ただし `use_figma`（書き込み）は使わない。
  - どうしてもFigma生成が必要になったら、**勝手にやらずユーザーに確認**し、実行時は**必ずファイルURLを明示**する（指定しないとCLAUDE.md既定のCTA-14019ファイルに書き込んでしまう）。
- ❌ `~/Desktop/CTA　デザイン/docs/measures/CTA-14019/`（デザイン施策側の作業フォルダ）
- ❌ `.claude/references/figma-design-rules.md` / `ct-token-reference.md` / `css-to-autolayout-mapping.md`（読むのは可、**編集しない**）
- ❌ `CLAUDE.md`（施策切替等の変更はユーザー主導。勝手に書き換えない）

## 3. 共有ファイルの扱い（唯一の衝突ポイント）

### `MEMORY.md`（`.claude/projects/.../memory/MEMORY.md`）
- 索引ファイルで両Claudeが追記しうる。**全体上書き厳禁**。
- 編集する場合は「**直前に最新を読み込む → 自分の1行だけ末尾に追加**」を厳守（他の行を消さない）。
- できれば、もう一方が能動的に編集していないタイミングで行う。

### `memory/*.md`（個別メモリ）
- **新規ファイルの作成はOK**。他人が作った既存メモリファイルは編集しない。

## 4. git（`cta-measure-learnings`）の同期ルール

- **正はリポジトリ**（`docs/引き継ぎ書.md` §2）。ミラー運用は廃止。編集は直接リポジトリで行い、commit/push する。
- 作業開始時と push 前に `git status` / `git log` を確認。**リモートが進んでいたら先に pull** して取り込む。
- commit user: `Haruya Hayashi <haruya.hayashi@leverages.jp>`。push は学習サイトの成果物のみ。

## 5. その他

- 一時ファイル（スクショ変換スクリプト等）は各セッション固有の scratchpad を使い、共有tmpに置かない。
- 事実とAI推論の区別（`factuals` / `hypothesisInsights`）を崩さない（引き継ぎ書・CLAUDE.md方針）。
- 迷ったら、他Claudeの担当に踏み込む前にユーザーへ確認する。

## レーン早見表

| 対象 | 施策学習サイト担当（あなた） | デザイナー担当（もう一方） |
|---|---|---|
| Figma `LfO02...`（CTA-14019） | ❌ 書き込み禁止（読取のみ） | ✅ 担当 |
| `cta-measure-learnings/`（リポジトリ全体） | ✅ 担当 | ❌ |
| `CTA　デザイン/docs/measures/CTA-14019/` | ❌ | ✅ 担当 |
| `.claude/references/measure-*` | ✅ 編集可 | 読むのみ |
| `.claude/references/figma/token/css-*` | 読むのみ | ✅ 編集可 |
| `MEMORY.md` | 末尾に自分の1行のみ追記 | 同左 |
| `CLAUDE.md` | ❌ 編集しない | ❌（ユーザー主導） |
